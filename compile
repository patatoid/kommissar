# app_root/compile
cd $phoenix_dir/assets/kommissar
NODE_ENV=dev npm ci
NODE_ENV=dev npm run build
cd $phoenix_dir
mix "${phoenix_ex}.digest" #use the ${phoenix_ex} variable instead of hardcoding phx or phoenix
